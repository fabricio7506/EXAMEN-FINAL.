<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permito respaldos automaticos para XML. define las configuraciones generales
    de la aplicación Android, incluyendo actividades, íconos, temas y permisos. -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PAMYAT"
        tools:targetApi="31">

        <!-- Declaración de la actividad principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="PAMYAT"
            android:theme="@style/Theme.PAMYAT">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declaración de GameActivity -->
        <activity
            android:name=".GameActivity"
            android:exported="true"
            android:label="Tablero del Juego"
            android:theme="@style/Theme.PAMYAT" />

    </application>

</manifest>
